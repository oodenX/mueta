# Plan: Configure CLI Defaults (v0.3.2)

## Phase 1: Verify and Update Defaults
- [x] Task: Inspect `src/mueta/cli/commands.py` to check current default for `--embedded`. 2e79a8d
- [x] Task: If default is `True`, change it to `False`. If it's `False`, verify functionality. 2e79a8d
- [ ] Task: Conductor - User Manual Verification 'Verify and Update Defaults' (Protocol in workflow.md)
